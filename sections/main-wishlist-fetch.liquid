

{% assign total=0 %}
{%for variant in product.variants %}
{% capture i %}{{ total | plus:variant.inventory_quantity }}{%endcapture%}
{% assign total = i %}
{%endfor%}  



thisIsaUniqueStringToSplitTheFollowingJsonObject!DoNotDelete


{
  "product_title": "{{ product.title }}", 
  "image": "{{ product.media[0].src | img_url: "800x" }}",
  "price": "{{ product.price | money }}",
  "rawprice": "{{ product.price }}",
  "url": "{{ product.url }}",
  "qty": "{{ total }}",
  "varId": "{{ product.variants[0].id }}",
  "sku": "{{ product.variants[0].sku }}",
  "diameter": "{{ product.metafields.pdf.diameter }} mm",
  "length": "{{ product.metafields.pdf.length }} mm",
  "height": "{{ product.metafields.pdf.height }} mm",
  "width": "{{ product.metafields.pdf.width }} mm",
  "weight": "{{ product.metafields.pdf.weight }} kg",
  "colour": "{{ product.metafields.pdf.colour }}",
  "era": "{{ product.metafields.custom.era.value }}",
  "origin": "{{ product.metafields.custom.origin.value }}",
  "description": "{{ product.description | escape }}",
  "type": "{{ product.type }}"
}


thisIsaUniqueStringToSplitTheFollowingJsonObject!DoNotDelete

